int main()
{
   int a,b,c;
   scanf("%d%d%d", &a, &b, &c);
   int max = 0;
   if (a > b)
   {
	   max = a;
   }
   else if(a<b)
   {
	   max = b;

   }
   if (c > max)
   {
	   max = c;
   }
   printf("max=%d", max);

	   return 0;
}
